name: CI/CD Pipeline

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  setup-and-static-analysis:
    uses: ./.github/workflows/static-analysis.yml
    permissions:
      contents: read
      packages: read
      actions: read
      checks: read
      deployments: read
      discussions: read
      issues: read
      pages: read
      pull-requests: read
      repository-projects: read
      statuses: read
      security-events: read
      id-token: read
