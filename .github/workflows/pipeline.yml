name: CI/CD Pipeline

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  static-code-analysis:
    uses: ./.github/workflows/static-code-analysis.yml
    permissions: read-all